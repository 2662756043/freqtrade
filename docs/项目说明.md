# Freqtrade 项目目录结构详解

本文档详细介绍 Freqtrade 项目的完整目录结构和各模块功能。

## 项目概览

Freqtrade 是一个使用 Python 编写的开源加密货币交易机器人（需要 Python >= 3.11）。它支持回测、超参数优化、实盘/模拟交易，并通过 CCXT 集成多个交易所。项目还包含一个 AI/ML 模块（FreqAI），用于基于机器学习的交易策略。

- **版本**：2026.7-dev
- **许可证**：GPLv3
- **Python 要求**：>= 3.11

---

## 根目录结构

```
freqtrade/
├── .devcontainer/      # VS Code 开发容器配置
├── .github/            # GitHub CI/CD 和模板
├── .idea/              # JetBrains IDE 配置
├── .vscode/            # VS Code 配置
├── build_helpers/      # 构建辅助脚本
├── config_examples/    # 配置文件示例
├── docker/             # Docker 相关文件
├── docs/               # 项目文档（MkDocs）
├── freqtrade/          # 主程序包
├── ft_client/          # REST API 客户端库
├── scripts/            # 工具脚本
├── tests/              # 测试套件
└── user_data/          # 用户数据目录
```

---

## 根目录详细说明

### `.devcontainer/`

VS Code Dev Container 配置，用于标准化开发环境。

- `devcontainer.json` — 定义开发容器设置（镜像、扩展、设置）

### `.github/`

GitHub 相关配置，包括 CI/CD 工作流、Issue 模板和赞助信息。

- **`ISSUE_TEMPLATE/`** — Issue 模板
  - `bug_report.md` — Bug 报告模板
  - `feature_request.md` — 功能请求模板
  - `question.md` — 问题咨询模板
- **`PULL_REQUEST_TEMPLATE.md`** — PR 模板
- **`workflows/`** — CI/CD 流水线定义
  - `ci.yml` — 主 CI 流水线（测试、代码检查）
  - `docker-build.yml` — Docker 镜像构建
  - `deploy-docs.yml` — 文档部署
  - `binance-lev-tier-update.yml` — 自动更新 Binance 杠杆层级
- `FUNDING.yml` — GitHub Sponsors 配置
- `dependabot.yml` — 依赖自动更新配置

### `build_helpers/`

构建时的辅助脚本和 ARM 平台的预编译包。

- `binance_update_lev_tiers.py` — 更新 Binance 杠杆层级数据
- `create_command_partials.py` — 生成命令文档片段
- `extract_config_json_schema.py` — 从配置中提取 JSON Schema
- `schema.json` — 生成的配置验证 JSON Schema
- `*.whl` — ARM 平台的预编译 wheel 包（pyarrow, ta-lib）

### `config_examples/`

不同交易所和使用场景的示例配置文件。

- `config_full.example.json` — 完整配置（包含所有选项的注释）
- `config_binance.example.json` — Binance 专用配置
- `config_kraken.example.json` — Kraken 专用配置
- `config_freqai.example.json` — FreqAI 专用配置

### `docker/`

各种部署场景的 Dockerfile 和 Compose 文件。

- `Dockerfile.armhf` — ARM（树莓派）构建
- `Dockerfile.custom` — 自定义构建（含用户扩展）
- `Dockerfile.freqai` — 包含 FreqAI 依赖的构建
- `Dockerfile.freqai_rl` — 包含 FreqAI + 强化学习的构建
- `Dockerfile.jupyter` — Jupyter Notebook 集成
- `Dockerfile.plot` — 绘图支持
- `docker-compose-freqai.yml` — FreqAI 的 Compose 文件
- `docker-compose-jupyter.yml` — Jupyter 的 Compose 文件

### `docs/`

基于 MkDocs 的项目文档（部署到 freqtrade.io）。

- **`assets/`** — 图片和截图
- **`commands/`** — 各命令的详细文档
- **`zh/`** — 中文翻译文档
- 顶层 `.md` 文件涵盖：配置、回测、机器人基础、策略开发、交易所设置、FreqAI、超参优化、数据分析等

### `ft_client/`

独立的 REST API 客户端库（可作为单独的 PyPI 包发布）。

- **`freqtrade_client/`**
  - `ft_client.py` — 主客户端类
  - `ft_rest_client.py` — REST API 客户端实现
- `pyproject.toml`, `requirements.txt` — 包配置

### `scripts/`

开发和测试用的工具脚本。

- `rest_client.py` — CLI REST 客户端，用于与运行中的机器人交互
- `ws_client.py` — WebSocket 客户端，用于测试 WS 连接

### `tests/`

按模块组织的完整测试套件。

- **子目录镜像主包结构**：`commands/`, `data/`, `exchange/`, `freqai/`, `optimize/`, `persistence/`, `plugins/`, `rpc/`, `strategy/`, `util/`
- **`testdata/`** — 测试数据（OHLCV 数据、配置文件、回测结果）
- **`exchange_online/`** — 在线交易所集成测试
- **`strategy/strats/`** — 测试策略实现

### `user_data/`

用户特定的数据目录（通过 `create-userdir` 命令创建）。

- `backtest_results/` — 回测结果存储
- `data/` — 下载的市场数据
- `freqaimodels/` — 自定义 FreqAI 模型
- `hyperopts/` — 自定义超参优化损失函数
- `logs/` — 日志文件
- `notebooks/` — Jupyter Notebook
- `strategies/` — 用户策略文件

---

## 主程序包：`freqtrade/`

### 核心文件（`freqtrade/*.py`）

| 文件 | 功能 |
|------|------|
| `__init__.py` | 包初始化，版本检测（开发版附加 git hash） |
| `__main__.py` | `python -m freqtrade` 的入口点 |
| `main.py` | CLI 入口：解析参数，分发到子命令 |
| `freqtradebot.py` | **核心机器人类**（`FreqtradeBot`）— 编排交易循环、订单管理、币对评估、入场/出场逻辑 |
| `worker.py` | `Worker` 类 — 封装 `FreqtradeBot`，处理生命周期（初始化、运行循环、重新配置、心跳） |
| `constants.py` | 全局常量（默认配置、订单类型、币对列表、超参损失函数等） |
| `exceptions.py` | 异常层次结构：`FreqtradeException` -> `OperationalException`, `DependencyException`, `ExchangeError` 等 |
| `wallets.py` | `Wallets` 类 — 跟踪可用/已用/总余额和持仓 |
| `misc.py` | 工具函数：JSON I/O、深度合并字典、币对转文件名等 |

---

### `freqtrade/commands/`

CLI 命令实现。每个文件处理一个或多个子命令。

| 文件 | 功能 |
|------|------|
| `arguments.py` | `Arguments` 类 — 基于 argparse 的 CLI 参数解析 |
| `cli_options.py` | 所有 CLI 选项的定义 |
| `trade_commands.py` | `start_trading()` — 启动实盘/模拟交易 |
| `data_commands.py` | 数据下载、转换、列表等命令 |
| `optimize_commands.py` | `start_backtesting()`, `start_hyperopt()` — 回测和超参优化 |
| `hyperopt_commands.py` | 超参结果的列表和详情查看 |
| `list_commands.py` | 列出交易所、市场、时间框架、策略等 |
| `deploy_commands.py` | 创建用户目录、新建策略、安装 UI |
| `build_config_commands.py` | 交互式配置构建器 |
| `plot_commands.py` | 绘图命令 |
| `analyze_commands.py` | 回测后的入场/出场分析 |
| `webserver_commands.py` | 独立 API/Web 服务器模式 |

---

### `freqtrade/config_schema/`

配置验证的 JSON Schema 定义。

- `config_schema.py` — 完整的 JSON Schema，定义每种模式的必需字段

---

### `freqtrade/configuration/`

配置加载、验证和管理。

| 文件 | 功能 |
|------|------|
| `configuration.py` | `Configuration` 类 — 主配置加载器，合并 CLI 参数、配置文件、环境变量 |
| `config_validation.py` | 配置验证（对照 JSON Schema） |
| `config_secrets.py` | 脱敏处理（移除交易所凭证） |
| `load_config.py` | JSON/TOML 配置文件读取 |
| `environment_vars.py` | 环境变量映射（`FREQTRADE__*`） |
| `timerange.py` | `TimeRange` 类 — 解析时间范围字符串（如 `20230101-20231231`） |

---

### `freqtrade/data/`

数据管理 — 下载、存储、转换和提供市场数据。

#### 核心文件

| 文件 | 功能 |
|------|------|
| `dataprovider.py` | `DataProvider` 类 — 策略的中央数据访问层，提供 OHLCV、订单簿、Ticker 数据 |
| `metrics.py` | 性能指标：最大回撤、Sharpe、Sortino、Calmar 等 |
| `entryexitanalysis.py` | 回测后的入场/出场信号分析 |

#### `data/history/` — 历史数据

| 文件 | 功能 |
|------|------|
| `history_utils.py` | 核心函数：`load_pair_history()`, `download_data_main()` |
| **`datahandlers/`** | 可插拔的数据存储后端 |
| `idatahandler.py` | 抽象基类 |
| `jsondatahandler.py` | JSON 格式存储 |
| `featherdatahandler.py` | Apache Feather 格式 |
| `parquetdatahandler.py` | Apache Parquet 格式 |
| `arrowdatahandler.py` | Apache Arrow 格式 |

#### `data/converter/` — 数据转换

| 文件 | 功能 |
|------|------|
| `converter.py` | OHLCV 转 DataFrame、清理、裁剪 |
| `trade_converter.py` | 交易数据格式转换 |
| `orderflow.py` | 订单流数据处理 |

#### `data/btanalysis/` — 回测分析

| 文件 | 功能 |
|------|------|
| `bt_fileutils.py` | 回测结果加载和管理 |
| `trade_parallelism.py` | 并行交易分析 |

---

### `freqtrade/enums/`

项目中使用的枚举类型。

| 文件 | 功能 |
|------|------|
| `runmode.py` | `RunMode`：LIVE, DRY_RUN, BACKTEST, HYPEROPT 等 |
| `state.py` | `State`：RUNNING, STOPPED |
| `tradingmode.py` | `TradingMode`：SPOT, MARGIN, FUTURES |
| `marginmode.py` | `MarginMode`：CROSS, ISOLATED |
| `candletype.py` | `CandleType`：SPOT, FUNDING_RATE, MARK |
| `signaltype.py` | `SignalType`, `SignalDirection`（LONG/SHORT） |
| `exittype.py` | `ExitType`：EXIT_SIGNAL, STOP_LOSS, TRAILING_STOP_LOSS 等 |
| `rpcmessagetype.py` | RPC 消息类型枚举 |

---

### `freqtrade/exchange/`

交易所抽象层。封装 CCXT 并添加交易所特定的自定义逻辑。

#### 核心文件

| 文件 | 功能 |
|------|------|
| `exchange.py` | `Exchange` 类 — 主交易所抽象（2000+ 行），处理订单创建/取消、余额获取、OHLCV 获取、WebSocket、精度处理 |
| `exchange_types.py` | 类型定义：`CcxtOrder`, `CcxtBalances`, `LeverageTier` 等 |
| `exchange_utils.py` | 工具函数：精度处理、交易所列表 |
| `exchange_utils_timeframe.py` | 时间框架工具函数 |
| `exchange_ws.py` | `ExchangeWS` 类 — 通过 CCXT Pro 的 WebSocket 集成 |
| `common.py` | API 重试装饰器 |

#### 交易所特定子类

每个文件扩展 `Exchange` 类，添加交易所特定行为：

| 文件 | 交易所 |
|------|--------|
| `binance.py` | Binance（现货、USD-M、美国版） |
| `kraken.py` | Kraken |
| `krakenfutures.py` | Kraken Futures |
| `bybit.py` | Bybit |
| `okx.py` | OKX |
| `gate.py` | Gate.io |
| `kucoin.py` | KuCoin |
| `htx.py` | HTX（火币） |
| `bingx.py` | BingX |
| `bitget.py` | Bitget |
| 其他 | BitMart, Bitpanda, Bitvavo, CoinEx, Crypto.com, HitBTC, Hyperliquid, IDEX, LBank, Luno, ModeTrade |

---

### `freqtrade/freqai/`

机器学习/AI 模块，用于训练和预测模型。

#### 核心文件

| 文件 | 功能 |
|------|------|
| `freqai_interface.py` | `IFreqaiModel` — 所有 FreqAI 模型的抽象基类 |
| `data_drawer.py` | `FreqaiDataDrawer` — 训练模型的持久化存储 |
| `data_kitchen.py` | `FreqaiDataKitchen` — 每币对的数据处理：特征工程、训练/测试拆分、归一化 |

#### `freqai/base_models/` — 基础模型类

- `BaseRegressionModel` — 回归模型基类
- `BaseClassifierModel` — 分类模型基类
- `BasePyTorchModel` — PyTorch 模型基类
- `FreqaiMultiOutputRegressor/Classifier` — 多输出包装器

#### `freqai/prediction_models/` — 具体模型实现

| 模型 | 类型 |
|------|------|
| `LightGBMRegressor` | LightGBM 回归 |
| `LightGBMClassifier` | LightGBM 分类 |
| `XGBoostRegressor` | XGBoost 回归 |
| `XGBoostClassifier` | XGBoost 分类 |
| `XGBoostRFRegressor` | XGBoost 随机森林回归 |
| `SKLearnRandomForestClassifier` | Scikit-learn 随机森林分类 |
| `PyTorchMLPRegressor/Classifier` | PyTorch MLP |
| `PyTorchTransformerRegressor` | PyTorch Transformer |
| `ReinforcementLearner` | 强化学习代理 |

#### `freqai/RL/` — 强化学习

- `BaseEnvironment` — 基础 RL 交易环境
- `Base3ActionRLEnv` — 3 动作环境（买/卖/持有）
- `Base4ActionRLEnv` — 4 动作环境
- `Base5ActionRLEnv` — 5 动作环境

#### `freqai/torch/` — PyTorch 训练基础设施

- `PyTorchMLPModel` — MLP 架构定义
- `PyTorchTransformerModel` — Transformer 架构定义
- `PyTorchModelTrainer` — 训练循环实现

#### `freqai/tensorboard/` — TensorBoard 集成

- 训练指标的可视化回调

---

### `freqtrade/ft_types/`

类型定义（TypedDict），用于结构化数据传递。

- `backtest_result_type.py` — 回测结果类型
- `plot_annotation_type.py` — 图表标注类型
- `valid_exchanges_type.py` — 有效交易所类型

---

### `freqtrade/leverage/`

杠杆和保证金计算。

- `interest.py` — 保证金利息计算
- `liquidation_price.py` — 强平价格计算（逐仓/全仓模式）

---

### `freqtrade/loggers/`

日志配置和自定义处理器。

| 文件 | 功能 |
|------|------|
| `__init__.py` | 日志配置（Rich 控制台输出、文件轮转、syslog） |
| `ft_rich_handler.py` | 自定义 Rich 日志处理器 |
| `buffering_handler.py` | 内存日志缓冲（用于 `/log` API） |
| `json_formatter.py` | JSON 日志格式化器 |

---

### `freqtrade/mixins/`

混入类，用于横切关注点。

- `logging_mixin.py` — `LoggingMixin` — 限速日志，避免日志洪泛

---

### `freqtrade/optimize/`

回测引擎、超参数优化和分析工具。

#### 核心文件

| 文件 | 功能 |
|------|------|
| `backtesting.py` | `Backtesting` 类 — 完整的回测引擎（2000+ 行） |
| `backtest_caching.py` | 策略运行 ID 缓存 |
| `hyperopt_tools.py` | 超参结果解析和显示 |

#### `optimize/hyperopt/` — 超参数优化

| 文件 | 功能 |
|------|------|
| `hyperopt.py` | `Hyperopt` 类 — 主优化编排器 |
| `hyperopt_optimizer.py` | `HyperOptimizer` — 执行单次优化试验 |
| `hyperopt_interface.py` | `IHyperOpt` — 抽象接口 |
| `hyperopt_auto.py` | `HyperOptAuto` — 自动使用策略定义的参数 |

#### `optimize/hyperopt_loss/` — 损失函数

12 个内置损失函数：

- `SharpeHyperOptLoss` — Sharpe 比率
- `SortinoHyperOptLoss` — Sortino 比率
- `CalmarHyperOptLoss` — Calmar 比率
- `MaxDrawDownHyperOptLoss` — 最大回撤
- `OnlyProfitHyperOptLoss` — 纯利润
- `ProfitDrawDownHyperOptLoss` — 利润+回撤平衡
- `MultiMetricHyperOptLoss` — 多指标组合
- 等等

#### `optimize/analysis/` — 策略分析

- `lookahead.py` — 前瞻偏差检测
- `recursive.py` — 递归偏差检测

#### `optimize/optimize_reports/` — 回测报告

- 回测结果的格式化和存储

---

### `freqtrade/persistence/`

数据库模型和交易持久化（SQLAlchemy ORM）。

| 文件 | 功能 |
|------|------|
| `trade_model.py` | `Trade`/`LocalTrade` 和 `Order` 模型 — 核心交易和订单数据库模型 |
| `pairlock.py` | `PairLock` 模型 — 币对锁定 |
| `wallet_history.py` | `WalletHistory` 模型 — 每日钱包余额跟踪 |
| `custom_data.py` | 每笔交易的键/值元数据存储 |
| `key_value_store.py` | 全局键/值存储 |
| `models.py` | `init_db()` — 数据库初始化 |
| `migrations.py` | 数据库迁移逻辑 |

---

### `freqtrade/plot/`

基于 Plotly 的绘图工具。

- `plotting.py` — 生成交互式 HTML 图表（K 线图、收益图）

---

### `freqtrade/plugins/`

插件系统，用于币对列表和保护机制。

#### `plugins/pairlist/` — 币对列表

| 类型 | 插件 |
|------|------|
| **选择器** | `StaticPairList`（静态）, `VolumePairList`（成交量）, `PercentChangePairList`（涨跌幅）, `MarketCapPairList`（市值）, `RemotePairList`（远程）, `ProducerPairList`（生产者） |
| **过滤器** | `AgeFilter`（上市时间）, `PriceFilter`（价格范围）, `SpreadFilter`（买卖价差）, `VolatilityFilter`（波动率）, `PerformanceFilter`（历史表现）, `ShuffleFilter`（随机排序）等 |

#### `plugins/protections/` — 保护机制

- `CooldownPeriod` — 亏损后暂停交易
- `LowProfitPairs` — 锁定低收益币对
- `MaxDrawDown` — 最大回撤时停止交易
- `StoplossGuard` — 止损过多时停止交易

---

### `freqtrade/resolvers/`

动态类加载器，用于策略、交易所和插件。

- `StrategyResolver` — 加载策略类
- `ExchangeResolver` — 加载交易所类
- `FreqaiModelResolver` — 加载 FreqAI 模型
- `HyperOptResolver` — 加载超参损失函数
- `PairListResolver` — 加载币对列表处理器
- `ProtectionResolver` — 加载保护处理器

---

### `freqtrade/rpc/`

远程过程调用系统，用于机器人通信（Telegram、REST API、Webhook、Discord）。

#### 核心文件

| 文件 | 功能 |
|------|------|
| `rpc.py` | `RPC` 类 — 核心 RPC 逻辑，实现所有查询/控制操作 |
| `rpc_manager.py` | `RPCManager` — 管理所有 RPC 处理器 |
| `telegram.py` | `Telegram` 类 — 完整的 Telegram 集成（2000+ 行） |
| `webhook.py` | `Webhook` 类 — HTTP 端点通知 |
| `discord.py` | `Discord` 类 — Discord Webhook 通知 |
| `fiat_convert.py` | 加密货币到法币的转换（使用 CoinGecko API） |
| `external_message_consumer.py` | 连接外部机器人的 WebSocket 消费数据 |

#### `rpc/api_server/` — REST API 服务器

基于 FastAPI 的 REST API 和 WebSocket 支持。

| 文件 | 功能 |
|------|------|
| `webserver.py` | `ApiServer` 类 — FastAPI 应用设置 |
| `api_v1.py` | V1 API 路由定义 |
| `api_auth.py` | JWT 认证端点 |
| `api_trading.py` | 交易控制端点 |
| `api_backtest.py` | 回测端点 |
| `api_schemas.py` | Pydantic 请求/响应模型 |
| **`ws/`** | WebSocket 基础设施 |

---

### `freqtrade/strategy/`

策略接口和策略相关工具。

| 文件 | 功能 |
|------|------|
| `interface.py` | `IStrategy` — **主策略接口**（1000+ 行），定义所有策略属性和抽象方法 |
| `hyper.py` | `HyperStrategyMixin` — 可超参优化的策略混入 |
| `parameters.py` | `IntParameter`, `DecimalParameter`, `BooleanParameter`, `CategoricalParameter`, `RealParameter` — 可优化参数定义 |
| `informative_decorator.py` | `@informative` 装饰器 — 声明式定义信息币对指标 |
| `strategy_helper.py` | `merge_informative_pair()` — 合并信息时间框架数据 |
| `strategy_validation.py` | 策略结果验证器 |
| `strategyupdater.py` | `StrategyUpdater` — 自动迁移 v2 策略到 v3 |

---

### `freqtrade/system/`

系统级配置和性能调优。

- `asyncio_config.py` — asyncio 事件循环策略配置
- `gc_setup.py` — 垃圾回收调优
- `set_mp_start_method.py` — 多进程启动方式配置
- `version_info.py` — 打印 Python、OS 和包版本

---

### `freqtrade/templates/`

策略和配置生成的模板。

- `sample_strategy.py` — 完整的示例策略
- `sample_hyperopt_loss.py` — 示例超参损失函数
- `FreqaiExampleStrategy.py` — FreqAI 示例策略
- `FreqaiExampleHybridStrategy.py` — FreqAI 混合示例策略
- `base_strategy.py.j2` — `new-strategy` 命令的 Jinja2 模板
- `base_config.json.j2` — `new-config` 命令的 Jinja2 模板
- **`strategy_subtemplates/`** — 策略子模板（指标、买卖趋势等）
- **`subtemplates/`** — 交易所特定配置模板

---

### `freqtrade/util/`

工具函数和辅助类。

| 文件 | 功能 |
|------|------|
| `datetime_helpers.py` | 日期时间工具函数 |
| `formatters.py` | 格式化函数（金额、百分比、持续时间） |
| `ft_precise.py` | `FtPrecise` — 精确的十进制算术 |
| `ft_ttlcache.py` | TTL 缓存实现 |
| `measure_time.py` | 代码计时上下文管理器 |
| `coin_gecko.py` | CoinGecko API 包装器 |
| `template_renderer.py` | Jinja2 模板渲染 |
| `rich_tables.py` | Rich 表格格式化 |

---

### `freqtrade/vendor/`

第三方代码（已弃用）。

- `qtpylib/indicators.py` — **已弃用** — 从 `technical.qtpylib` 重新导出，带有弃用警告

---

## 关键架构模式

### 1. 插件架构

策略、交易所、币对列表、保护机制、超参损失函数和 FreqAI 模型都通过解析器（resolvers）动态加载。

### 2. 配置级联

配置通过合并构建：CLI 参数 -> 配置文件 -> 环境变量 -> 默认值，并通过 JSON Schema 验证。

### 3. 数据提供者模式

`DataProvider` 作为中央数据访问层，抽象了实盘/历史/生产者数据源。

### 4. RPC 抽象

`RPCManager` 通过通用的 `RPCHandler` 接口管理多个通信渠道（Telegram、Webhook、Discord、REST API）。

### 5. ORM 持久化

使用 SQLAlchemy 模型（`Trade`, `Order`, `PairLock`, `WalletHistory`），支持自动迁移。

### 6. 交易所抽象

基于 CCXT 的 `Exchange` 类，每个交易所都有特定的子类来处理平台特定行为。

---

## 数据流概览

```
┌─────────────────────────────────────────────────────────────┐
│                        CLI / API                            │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                    Worker (worker.py)                        │
│              管理机器人生命周期和心跳                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│               FreqtradeBot (freqtradebot.py)                 │
│         核心交易循环：评估币对、管理订单、执行策略              │
└────────┬──────────────┬─────────────────┬───────────────────┘
         │              │                 │
┌────────▼──────┐ ┌─────▼──────┐ ┌───────▼──────────────────┐
│   Strategy    │ │  Exchange   │ │     DataProvider         │
│  (strategy/)  │ │ (exchange/) │ │      (data/)             │
│  策略逻辑     │ │  交易所API  │ │   市场数据访问            │
└───────────────┘ └────────────┘ └──────────────────────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
              ┌─────▼──────┐  ┌───────▼──────┐  ┌───────▼──────┐
              │  History    │  │   Ticker     │  │   Producer   │
              │  历史数据   │  │  实时价格    │  │  外部数据     │
              └────────────┘  └──────────────┘  └──────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Persistence (persistence/)                │
│              SQLAlchemy ORM：Trade, Order, PairLock          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    RPC (rpc/)                                │
│         Telegram / REST API / Webhook / Discord              │
└─────────────────────────────────────────────────────────────┘
```

---

## 快速导航

- **想写策略？** → `freqtrade/strategy/interface.py`（IStrategy 接口）
- **想了解回测？** → `freqtrade/optimize/backtesting.py`
- **想了解超参优化？** → `freqtrade/optimize/hyperopt/`
- **想添加交易所支持？** → `freqtrade/exchange/`
- **想使用 AI？** → `freqtrade/freqai/`
- **想修改 API？** → `freqtrade/rpc/api_server/`
- **想添加保护机制？** → `freqtrade/plugins/protections/`
- **想修改币对列表？** → `freqtrade/plugins/pairlist/`