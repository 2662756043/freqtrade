# Freqtrade 实战指南：从零开始到实盘交易

本文档是一份完整的实战指南，涵盖安装配置、策略开发、回测优化、参数调优和实盘部署的全流程。

---

## 目录

1. [环境准备与安装](#1-环境准备与安装)
2. [项目初始化](#2-项目初始化)
3. [配置文件详解](#3-配置文件详解)
4. [下载历史数据](#4-下载历史数据)
5. [编写你的第一个策略](#5-编写你的第一个策略)
6. [回测详解](#6-回测详解)
7. [超参数优化（调参）](#7-超参数优化调参)
8. [模拟交易（Dry-Run）](#8-模拟交易dry-run)
9. [实盘交易（Live Trading）](#9-实盘交易live-trading)
10. [Telegram 远程控制](#10-telegram-远程控制)
11. [FreqAI 机器学习策略](#11-freqai-机器学习策略)
12. [常见问题排查](#12-常见问题排查)

---

## 1. 环境准备与安装

### 1.1 系统要求

- **Python**：>= 3.11（推荐 3.12）
- **操作系统**：Windows 10/11、macOS、Linux
- **内存**：建议 8GB 以上（回测和超参优化需要较多内存）
- **磁盘**：至少 2GB 可用空间（历史数据）

### 1.2 安装方式

#### 方式一：pip 安装（推荐新手）

```bash
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

# 安装 freqtrade
pip install freqtrade

# 或者安装开发版
pip install freqtrade --pre
```

#### 方式二：从源码安装

```bash
# 克隆仓库
git clone https://github.com/freqtrade/freqtrade.git
cd freqtrade

# 创建虚拟环境
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# 或
.venv\Scripts\activate     # Windows

# 安装依赖
pip install -e .
```

### 1.3 安装 TA-Lib（推荐）

TA-Lib 是最常用的技术分析库，Freqtrade 的很多策略都依赖它。**强烈建议安装**，否则大量策略示例无法运行。

TA-Lib 分两部分：**C 语言库**（底层）和 **Python 包**（上层），必须先装 C 库再装 Python 包。

#### Windows 安装

**方法一：下载预编译 wheel（最简单，推荐）**

```powershell
# 从 GitHub 下载预编译的 wheel 文件，自动匹配 Python 版本
pip install ta-lib
```

如果直接安装失败（提示需要编译），手动下载 wheel：

1. 访问 https://github.com/cgohlke/talib-build/releases
2. 下载对应你 Python 版本的 `.whl` 文件，例如：
   - Python 3.12 64位：`ta_lib‑0.6.8‑cp312‑cp312‑win_amd64.whl`
   - Python 3.11 64位：`ta_lib‑0.6.8‑cp311‑cp311‑win_amd64.whl`
3. 安装：

```powershell
pip install ta_lib‑0.6.8‑cp312‑cp312‑win_amd64.whl
```

**方法二：手动安装 C 库 + pip**

1. 从 http://ta-lib.org 下载 Windows 版本的 TA-Lib C 库（`.zip` 文件）
2. 解压到 `C:\ta-lib`（路径不要有中文或空格）
3. 然后用 pip 安装 Python 包：

```powershell
pip install ta-lib
```

#### Linux 安装（Debian/Ubuntu）

```bash
# 安装编译依赖
sudo apt-get install -y build-essential wget

# 下载并编译 TA-Lib C 库
wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
tar -xzf ta-lib-0.4.0-src.tar.gz
cd ta-lib
./configure --prefix=/usr
make
sudo make install
cd ..

# 安装 Python 包
pip install ta-lib
```

#### macOS 安装

```bash
# 使用 Homebrew 安装 C 库
brew install ta-lib

# 安装 Python 包
pip install ta-lib
```

#### Conda 安装（全平台通用，最省心）

```bash
conda install -c conda-forge ta-lib
```

Conda 会自动处理 C 库依赖，无需手动编译。

#### 验证安装

```python
import talib
print(talib.__version__)
# 输出类似：0.6.8
```

如果没有报错，说明安装成功。

#### 常见安装问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `error: Microsoft Visual C++ 14.0 is required` | Windows 缺少 C++ 编译器 | 安装 [Visual C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) 或使用预编译 wheel |
| `ta-lib.c:xxx: fatal error: ta_libc.h: No such file` | C 库未安装或路径不对 | 先安装 TA-Lib C 库，再装 Python 包 |
| `numpy` 版本冲突 | numpy 版本不兼容 | 先升级 numpy：`pip install numpy --upgrade` |
| ARM 架构（M1/M2 Mac）编译失败 | 架构不兼容 | 使用 Conda 安装或 Docker |

#### 方式三：Docker 安装

```bash
# 拉取镜像
docker pull freqtradeorg/freqtrade:stable

# 或使用 docker-compose
git clone https://github.com/freqtrade/freqtrade.git
cd freqtrade
docker compose up -d
```

### 1.3 验证安装

```bash
freqtrade --version
# 输出类似：freqtrade 2026.7-dev
```

---

## 2. 项目初始化

### 2.1 创建用户数据目录

```bash
freqtrade create-userdir --userdir user_data
```

这会创建以下目录结构：

```
user_data/
├── backtest_results/    # 回测结果
├── data/                # 历史数据
├── freqaimodels/        # 自定义 FreqAI 模型
├── hyperopts/           # 自定义超参损失函数
├── hyperopt_results/    # 超参优化结果
├── logs/                # 日志文件
├── notebooks/           # Jupyter Notebook
└── strategies/          # 策略文件
```

### 2.2 生成配置文件

```bash
# 交互式创建配置
freqtrade new-config --config user_data/config.json

# 或者复制示例配置
cp config_examples/config_full.example.json user_data/config.json
```

### 2.3 生成策略模板

```bash
# 创建基础策略
freqtrade new-strategy --strategy MyStrategy

# 创建高级策略（包含所有回调方法）
freqtrade new-strategy --strategy MyStrategy --template advanced

# 创建 FreqAI 策略
freqtrade new-strategy --strategy MyFreqAIStrategy --freqaimodel LightGBMRegressor
```

---

## 3. 配置文件详解

配置文件 `config.json` 是 Freqtrade 的核心配置，控制机器人的所有行为。

### 3.1 最小配置

```json
{
    "trading_mode": "spot",
    "margin_mode": "",
    "max_open_trades": 3,
    "stake_currency": "USDT",
    "stake_amount": "unlimited",
    "tradable_balance_ratio": 0.99,
    "fiat_display_currency": "CNY",
    "dry_run": true,
    "dry_run_wallet": 1000,
    "cancel_open_orders_on_exit": false,
    "exchange": {
        "name": "binance",
        "key": "",
        "secret": "",
        "ccxt_sync_config": {},
        "ccxt_async_config": {},
        "pair_whitelist": [
            "BTC/USDT",
            "ETH/USDT"
        ],
        "pair_blacklist": []
    },
    "entry_pricing": {
        "price_side": "same",
        "use_order_book": true,
        "order_book_top": 1,
        "price_last_balance": 0.0,
        "check_depth_of_market": {
            "enabled": false,
            "bids_to_ask_delta": 1
        }
    },
    "exit_pricing": {
        "price_side": "same",
        "use_order_book": true,
        "order_book_top": 1
    },
    "pairlists": [
        {"method": "StaticPairList"}
    ],
    "bot_name": "freqtrade",
    "initial_state": "running",
    "internals": {
        "process_throttle_secs": 5
    }
}
```

### 3.2 关键配置说明

#### 交易模式

```json
{
    "trading_mode": "spot",      // spot: 现货, margin: 杠杆, futures: 合约
    "margin_mode": "",           // cross: 全仓, isolated: 逐仓
    "can_short": false           // 是否允许做空
}
```

#### 资金管理

```json
{
    "max_open_trades": 3,           // 最大同时持仓数
    "stake_currency": "USDT",       // 计价货币
    "stake_amount": "unlimited",    // 每笔交易金额，unlimited 表示按比例分配
    "tradable_balance_ratio": 0.99, // 可用余额比例
    "dry_run_wallet": 1000          // 模拟钱包金额（USDT）
}
```

#### 交易所配置

```json
{
    "exchange": {
        "name": "binance",           // 交易所名称
        "key": "your_api_key",       // API Key（实盘时填写）
        "secret": "your_api_secret", // API Secret（实盘时填写）
        "pair_whitelist": [          // 交易白名单
            "BTC/USDT",
            "ETH/USDT",
            "BNB/USDT"
        ],
        "pair_blacklist": [          // 交易黑名单
            "BNB/BTC"
        ]
    }
}
```

#### 价格设置

```json
{
    "entry_pricing": {
        "price_side": "same",      // same: 使用当前价格, other: 使用对手价
        "use_order_book": true,    // 使用订单簿定价
        "order_book_top": 1        // 使用订单簿第 N 档
    },
    "exit_pricing": {
        "price_side": "same",
        "use_order_book": true,
        "order_book_top": 1
    }
}
```

#### 币对列表

```json
{
    "pairlists": [
        {"method": "StaticPairList"},                    // 静态列表（使用 whitelist）
        {"method": "VolumePairList", "number_assets": 20}, // 按 24h 成交量选择前 20
        {"method": "PriceFilter", "low_price_ratio": 0.001}, // 价格过滤
        {"method": "SpreadFilter", "max_spread_ratio": 0.005} // 价差过滤
    ]
}
```

### 3.3 环境变量覆盖

任何配置都可以通过环境变量覆盖，格式为 `FREQTRADE__` + 配置路径：

```bash
# 覆盖交易所名称
export FREQTRADE__EXCHANGE__NAME=kraken

# 覆盖 dry_run 设置
export FREQTRADE__DRY_RUN=false

# 覆盖 stake_amount
export FREQTRADE__STAKE_AMOUNT=100
```

---

## 4. 下载历史数据

回测和超参优化都需要历史数据。Freqtrade 支持从多个交易所下载数据。

### 4.1 基本下载

```bash
# 下载默认时间框架的数据（使用配置中的 timeframe）
freqtrade download-data --exchange binance

# 下载指定时间框架
freqtrade download-data --exchange binance --timeframes 5m 15m 1h

# 下载指定时间范围
freqtrade download-data --exchange binance --timerange 20230101-20240101

# 下载指定币对
freqtrade download-data --exchange binance --pairs BTC/USDT ETH/USDT

# 下载所有白名单币对
freqtrade download-data --exchange binance --days 30
```

### 4.2 下载交易数据（Tick Data）

```bash
# 下载原始交易数据（用于生成自定义时间框架的 OHLCV）
freqtrade download-data --exchange binance --dl-trades

# 将交易数据转换为 OHLCV
freqtrade trades-to-ohlcv --exchange binance --timeframes 5m 15m 1h
```

### 4.3 查看已下载数据

```bash
# 列出已下载的数据
freqtrade list-data --exchange binance

# 列出已下载的交易数据
freqtrade list-trades-data --exchange binance
```

### 4.4 数据格式

Freqtrade 支持多种数据格式：

| 格式 | 说明 | 文件扩展名 |
|------|------|-----------|
| JSON | 默认格式，人类可读 | `.json` |
| Feather | Apache Arrow 格式，快速 | `.feather` |
| Parquet | 列式存储，压缩率高 | `.parquet` |

可以在配置中指定：

```json
{
    "dataformat_ohlcv": "feather",
    "dataformat_trades": "feather"
}
```

---

## 5. 编写你的第一个策略

### 5.1 策略基础结构

策略是一个 Python 类，继承自 `IStrategy`。必须实现以下方法：

```python
from freqtrade.strategy import IStrategy, IntParameter, DecimalParameter
from pandas import DataFrame
import talib.abstract as ta

class MyFirstStrategy(IStrategy):
    """
    我的第一个策略
    """

    # === 策略接口版本 ===
    INTERFACE_VERSION = 3

    # === 时间框架 ===
    timeframe = '5m'

    # === 最小 ROI（止盈） ===
    minimal_roi = {
        "0": 0.05,      # 5% 立即止盈
        "30": 0.03,     # 30 分钟后 3% 止盈
        "60": 0.01,     # 60 分钟后 1% 止盈
        "120": 0        # 120 分钟后保本退出
    }

    # === 止损 ===
    stoploss = -0.05    # 5% 止损

    # === 追踪止损 ===
    trailing_stop = True
    trailing_stop_positive = 0.01      # 盈利 1% 后启动追踪
    trailing_only_offset_is_reached = True

    # === 启动所需的 K 线数量 ===
    startup_candle_count = 50

    # === 可优化参数 ===
    buy_rsi = IntParameter(20, 40, default=30, space="buy")
    buy_adx = IntParameter(20, 50, default=25, space="buy")
    sell_rsi = IntParameter(60, 90, default=70, space="sell")

    def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        计算技术指标
        """
        # RSI
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)

        # ADX
        dataframe['adx'] = ta.ADX(dataframe, timeperiod=14)

        # EMA
        dataframe['ema_fast'] = ta.EMA(dataframe, timeperiod=12)
        dataframe['ema_slow'] = ta.EMA(dataframe, timeperiod=26)

        # MACD
        macd = ta.MACD(dataframe)
        dataframe['macd'] = macd['macd']
        dataframe['macdsignal'] = macd['macdsignal']

        return dataframe

    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        定义买入信号
        """
        dataframe.loc[
            (
                (dataframe['rsi'] < self.buy_rsi.value) &    # RSI 超卖
                (dataframe['adx'] > self.buy_adx.value) &    # 趋势有力度
                (dataframe['ema_fast'] > dataframe['ema_slow']) &  # 金叉
                (dataframe['volume'] > 0)                     # 有成交量
            ),
            'enter_long'] = 1

        return dataframe

    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        定义卖出信号
        """
        dataframe.loc[
            (
                (dataframe['rsi'] > self.sell_rsi.value) |   # RSI 超买
                (dataframe['ema_fast'] < dataframe['ema_slow'])  # 死叉
            ),
            'exit_long'] = 1

        return dataframe
```

### 5.2 策略关键属性

| 属性 | 说明 | 示例 |
|------|------|------|
| `INTERFACE_VERSION` | 策略接口版本，必须为 3 | `3` |
| `timeframe` | K 线时间框架 | `'5m'`, `'15m'`, `'1h'`, `'4h'`, `'1d'` |
| `minimal_roi` | 最小收益表（止盈） | `{"0": 0.1}` 表示 10% 立即止盈 |
| `stoploss` | 止损比例（负数） | `-0.1` 表示 10% 止损 |
| `trailing_stop` | 是否启用追踪止损 | `True` / `False` |
| `trailing_stop_positive` | 追踪止损触发点 | `0.01` 表示盈利 1% 后追踪 |
| `trailing_only_offset_is_reached` | 仅在达到偏移后追踪 | `True` / `False` |
| `can_short` | 是否支持做空 | `True` / `False` |
| `startup_candle_count` | 启动所需的 K 线数 | `50` |
| `process_only_new_candles` | 仅在新 K 线时处理 | `True`（推荐） |
| `use_exit_signal` | 使用策略退出信号 | `True` / `False` |
| `order_types` | 订单类型配置 | 见下文 |
| `order_time_in_force` | 订单有效期 | 见下文 |

### 5.3 订单类型配置

```python
order_types = {
    "entry": "limit",              # 入场：limit（限价）或 market（市价）
    "exit": "limit",               # 出场
    "stoploss": "market",          # 止损：建议使用 market
    "stoploss_on_exchange": False,  # 是否在交易所设置止损单
    "stoploss_on_exchange_interval": 60,  # 交易所止损单检查间隔（秒）
}

order_time_in_force = {
    "entry": "GTC",   # GTC: Good Till Cancelled
    "exit": "GTC"     # IOC: Immediate or Cancel, FOK: Fill or Kill
}
```

### 5.4 可优化参数类型

| 参数类型 | 说明 | 示例 |
|----------|------|------|
| `IntParameter` | 整数参数 | `IntParameter(20, 40, default=30, space="buy")` |
| `DecimalParameter` | 小数参数 | `DecimalParameter(0.01, 0.1, default=0.05, decimals=3)` |
| `RealParameter` | 浮点参数 | `RealParameter(0, 1, default=0.5)` |
| `BooleanParameter` | 布尔参数 | `BooleanParameter(default=True)` |
| `CategoricalParameter` | 分类参数 | `CategoricalParameter(["ema", "sma", "tema"])` |

参数的 `space` 属性决定它属于哪个搜索空间：
- `"buy"` — 买入参数
- `"sell"` — 卖出参数
- `"protection"` — 保护参数
- 自定义空间名

### 5.5 预设参数值

可以通过类属性预设参数值，跳过优化直接使用：

```python
class MyStrategy(IStrategy):
    # 预设买入参数
    buy_params = {
        "buy_rsi": 28,
        "buy_adx": 30,
    }

    # 预设卖出参数
    sell_params = {
        "sell_rsi": 72,
    }

    # 参数定义
    buy_rsi = IntParameter(20, 40, default=30, space="buy")
    buy_adx = IntParameter(20, 50, default=25, space="buy")
    sell_rsi = IntParameter(60, 90, default=70, space="sell")
```

### 5.6 常用技术指标

```python
def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
    # === 趋势指标 ===
    dataframe['ema'] = ta.EMA(dataframe, timeperiod=20)       # 指数移动平均
    dataframe['sma'] = ta.SMA(dataframe, timeperiod=20)       # 简单移动平均
    dataframe['tema'] = ta.TEMA(dataframe, timeperiod=20)     # 三重指数移动平均
    dataframe['macd'] = ta.MACD(dataframe)['macd']            # MACD
    dataframe['adx'] = ta.ADX(dataframe, timeperiod=14)       # ADX（趋势强度）
    dataframe['plus_di'] = ta.PLUS_DI(dataframe)              # +DI
    dataframe['minus_di'] = ta.MINUS_DI(dataframe)            # -DI

    # === 震荡指标 ===
    dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)       # RSI
    dataframe['mfi'] = ta.MFI(dataframe, timeperiod=14)       # MFI（资金流量）
    dataframe['cci'] = ta.CCI(dataframe, timeperiod=14)       # CCI
    stoch = ta.STOCHF(dataframe)                              # 随机指标
    dataframe['fastk'] = stoch['fastk']
    dataframe['fastd'] = stoch['fastd']

    # === 波动率指标 ===
    bollinger = ta.BBANDS(dataframe, timeperiod=20)           # 布林带
    dataframe['bb_lower'] = bollinger['lowerband']
    dataframe['bb_middle'] = bollinger['middleband']
    dataframe['bb_upper'] = bollinger['upperband']
    dataframe['atr'] = ta.ATR(dataframe, timeperiod=14)       # ATR（真实波幅）

    # === 成交量指标 ===
    dataframe['obv'] = ta.OBV(dataframe)                      # OBV（能量潮）
    dataframe['volume_mean'] = dataframe['volume'].rolling(20).mean()

    return dataframe
```

### 5.7 多时间框架策略

使用 `@informative` 装饰器轻松添加更高时间框架的数据：

```python
from freqtrade.strategy import IStrategy, informative

class MyMultiTimeframeStrategy(IStrategy):
    timeframe = '5m'

    @informative('1h')
    def populate_indicators_1h(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """1小时时间框架的指标"""
        dataframe['ema'] = ta.EMA(dataframe, timeperiod=20)
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)
        return dataframe

    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        # 使用 1h 数据：列名自动为 rsi_1h, ema_1h
        dataframe.loc[
            (
                (dataframe['rsi'] < 30) &
                (dataframe['rsi_1h'] < 40) &      # 1h RSI 也偏低
                (dataframe['ema_1h'] > 0)          # 1h EMA 存在
            ),
            'enter_long'] = 1
        return dataframe
```

---

## 6. 回测详解

回测是使用历史数据验证策略性能的关键步骤。

### 6.1 基本回测

```bash
# 基本回测
freqtrade backtesting --strategy MyStrategy

# 指定时间范围
freqtrade backtesting --strategy MyStrategy --timerange 20230101-20240101

# 指定时间框架
freqtrade backtesting --strategy MyStrategy --timeframe 1h

# 使用自定义配置
freqtrade backtesting --strategy MyStrategy --config user_data/config.json

# 指定数据目录
freqtrade backtesting --strategy MyStrategy --datadir user_data/data/binance
```

### 6.2 回测参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--strategy` | 策略类名 | `MyStrategy` |
| `--timerange` | 时间范围 | `20230101-20240101` |
| `--timeframe` | 时间框架 | `5m`, `1h` |
| `--config` | 配置文件 | `user_data/config.json` |
| `--datadir` | 数据目录 | `user_data/data/binance` |
| `--export` | 导出结果 | `trades` |
| `--export-filename` | 导出文件名 | `backtest_results/result.json` |
| `--stake-amount` | 每笔金额 | `100` |
| `--max-open-trades` | 最大持仓数 | `5` |
| `--dry-run-wallet` | 初始资金 | `1000` |
| `--enable-protections` | 启用保护机制 | 无需值 |
| `--cache` | 缓存策略结果 | `none`, `day`, `week` |

### 6.3 回测输出解读

```
==========================================================
 BACKTESTING REPORT
==========================================================
| Pair         | Trades | Win | Draw | Loss | Avg Profit |
|--------------|--------|-----|------|------|------------|
| BTC/USDT     |     45 |  28 |    0 |   17 |       2.1% |
| ETH/USDT     |     38 |  22 |    0 |   16 |       1.8% |
|--------------|--------|-----|------|------|------------|
| TOTAL        |     83 |  50 |    0 |   33 |       2.0% |

==========================================================
 SUMMARY METRICS
==========================================================
| Metric                     | Value          |
|----------------------------|----------------|
| Backtesting from           | 2023-01-01     |
| Backtesting to             | 2024-01-01     |
| Max open trades            | 3              |
|                            |                |
| Total/Daily Avg Profit     | 156.23 / 0.43  |
| Profit %                   | 15.62%         |
| Trades per day             | 0.23           |
| Avg. holding time          | 4:32:00        |
| Win Rate                   | 60.24%         |
|                            |                |
| Max Drawdown               | 8.45%          |
| Drawdown Start             | 2023-03-15     |
| Drawdown End               | 2023-04-02     |
|                            |                |
| Sharpe Ratio               | 1.23           |
| Sortino Ratio              | 1.89           |
| Calmar Ratio               | 1.85           |
| Expectancy                 | 0.02           |
| SQN                        | 1.45           |
==========================================================
```

关键指标说明：

| 指标 | 说明 | 好的值 |
|------|------|--------|
| **Profit %** | 总收益率 | > 10% |
| **Win Rate** | 胜率 | > 50% |
| **Max Drawdown** | 最大回撤 | < 15% |
| **Sharpe Ratio** | 夏普比率 | > 1.0 |
| **Sortino Ratio** | 索提诺比率 | > 1.5 |
| **Calmar Ratio** | 卡玛比率 | > 1.0 |
| **Expectancy** | 期望收益 | > 0 |
| **SQN** | 系统质量数 | > 2.0 |

### 6.4 回测结果分析

```bash
# 查看回测结果详情
freqtrade backtesting-show

# 分析入场/出场信号
freqtrade backtesting-analysis --config user_data/config.json

# 绘制回测图表
freqtrade plot-dataframe --strategy MyStrategy --timerange 20230101-20230201

# 绘制收益图
freqtrade plot-profit --strategy MyStrategy --timerange 20230101-20240101
```

### 6.5 回测注意事项

1. **数据质量**：确保数据完整，没有缺失的 K 线
2. **过拟合**：不要过度优化参数，会导致实盘表现差
3. **滑点和手续费**：回测已考虑，但实际可能更高
4. **前瞻偏差**：避免使用未来数据（如 `shift(-1)`）
5. **样本外测试**：用不同时间段验证策略

---

## 7. 超参数优化（调参）

Hyperopt 使用机器学习算法自动寻找最优参数。

### 7.1 基本用法

```bash
# 优化买入和卖出参数，运行 500 轮
freqtrade hyperopt --strategy MyStrategy --epochs 500 --spaces buy sell

# 指定损失函数
freqtrade hyperopt --strategy MyStrategy --epochs 500 --hyperopt-loss SharpeHyperOptLoss

# 优化所有空间
freqtrade hyperopt --strategy MyStrategy --epochs 500 --spaces all

# 限制 CPU 核心数
freqtrade hyperopt --strategy MyStrategy --epochs 500 --jobs 2

# 设置最少交易次数（过滤无效结果）
freqtrade hyperopt --strategy MyStrategy --epochs 500 --min-trades 20

# 启用早停（连续无改进时自动停止）
freqtrade hyperopt --strategy MyStrategy --epochs 500 --early-stop 50
```

### 7.2 搜索空间

| 空间 | 说明 | 包含的参数 |
|------|------|-----------|
| `buy` | 买入空间 | 所有 `space="buy"` 的参数 |
| `sell` | 卖出空间 | 所有 `space="sell"` 的参数 |
| `roi` | 止盈空间 | minimal_roi 表 |
| `stoploss` | 止损空间 | stoploss 值 |
| `trailing` | 追踪止损空间 | 追踪止损参数 |
| `protection` | 保护空间 | 保护机制参数 |
| `trades` | 交易数空间 | max_open_trades |
| `all` | 所有空间 | 以上全部 |
| `default` | 默认空间 | buy, sell, roi, stoploss |

```bash
# 只优化买入参数
freqtrade hyperopt --strategy MyStrategy --spaces buy

# 优化多个空间
freqtrade hyperopt --strategy MyStrategy --spaces buy sell roi stoploss

# 优化所有空间
freqtrade hyperopt --strategy MyStrategy --spaces all
```

### 7.3 损失函数

| 损失函数 | 说明 | 适用场景 |
|----------|------|----------|
| `ShortTradeDurHyperOptLoss` | 默认损失函数 | 综合考虑 |
| `OnlyProfitHyperOptLoss` | 纯利润 | 追求高收益 |
| `SharpeHyperOptLoss` | Sharpe 比率 | 风险调整收益 |
| `SharpeHyperOptLossDaily` | 每日 Sharpe | 日内策略 |
| `SortinoHyperOptLoss` | Sortino 比率 | 下行风险控制 |
| `SortinoHyperOptLossDaily` | 每日 Sortino | 日内策略 |
| `CalmarHyperOptLoss` | Calmar 比率 | 回撤控制 |
| `MaxDrawDownHyperOptLoss` | 最大回撤 | 最小化回撤 |
| `MaxDrawDownRelativeHyperOptLoss` | 相对回撤 | 相对回撤控制 |
| `ProfitDrawDownHyperOptLoss` | 利润+回撤 | 平衡收益和风险 |
| `MultiMetricHyperOptLoss` | 多指标组合 | 综合优化 |

```bash
# 查看所有可用损失函数
freqtrade list-hyperoptloss

# 使用 Sharpe 损失函数
freqtrade hyperopt --strategy MyStrategy --hyperopt-loss SharpeHyperOptLoss

# 使用利润+回撤平衡损失函数
freqtrade hyperopt --strategy MyStrategy --hyperopt-loss ProfitDrawDownHyperOptLoss
```

### 7.4 查看优化结果

```bash
# 列出所有优化结果
freqtrade hyperopt-list

# 只看最优结果
freqtrade hyperopt-list --best

# 查看某个 epoch 的详情
freqtrade hyperopt-show -n 1

# 导出为 CSV
freqtrade hyperopt-list --export-csv hyperopt_results.csv
```

### 7.5 应用优化结果

优化完成后，将最优参数复制到策略中：

```python
class MyStrategy(IStrategy):
    # 从 hyperopt-show 输出中复制的最优参数
    buy_params = {
        "buy_rsi": 28,
        "buy_adx": 32,
    }

    sell_params = {
        "sell_rsi": 72,
    }

    # 参数定义保持不变
    buy_rsi = IntParameter(20, 40, default=30, space="buy")
    buy_adx = IntParameter(20, 50, default=25, space="buy")
    sell_rsi = IntParameter(60, 90, default=70, space="sell")
```

### 7.6 自定义损失函数

```python
# user_data/hyperopts/my_loss.py
from datetime import datetime
from pandas import DataFrame
from freqtrade.optimize.hyperopt_loss import IHyperOptLoss

class MyCustomLoss(IHyperOptLoss):
    """
    自定义损失函数：平衡利润和回撤
    """

    @staticmethod
    def hyperopt_loss_function(
        *, results: DataFrame, trade_count: int,
        min_date: datetime, max_date: datetime,
        config: dict, processed: dict, backtest_stats: dict,
        starting_balance: float, **kwargs
    ) -> float:
        """
        返回越小越好的值
        """
        total_profit = results['profit_ratio'].sum()
        max_drawdown = backtest_stats.get('max_drawdown_account', 0)

        # 最大化利润，最小化回撤
        loss = -total_profit + (max_drawdown * 10)

        return loss
```

使用自定义损失函数：

```bash
freqtrade hyperopt --strategy MyStrategy --hyperopt-loss MyCustomLoss
```

### 7.7 超参优化最佳实践

1. **先用少量 epoch 测试**：确认参数空间正确
2. **分阶段优化**：先优化 buy，再优化 sell，最后优化 roi/stoploss
3. **使用早停**：避免浪费时间在无改进的搜索上
4. **验证结果**：用不同时间段回测优化后的参数
5. **避免过拟合**：参数范围不要太窄，epoch 不要太多

```bash
# 第一阶段：优化买入参数
freqtrade hyperopt --strategy MyStrategy --spaces buy --epochs 300

# 第二阶段：优化卖出参数
freqtrade hyperopt --strategy MyStrategy --spaces sell --epochs 300

# 第三阶段：优化止损和止盈
freqtrade hyperopt --strategy MyStrategy --spaces roi stoploss --epochs 200

# 最终验证：用优化后的参数在不同时间段回测
freqtrade backtesting --strategy MyStrategy --timerange 20240101-20240601
```

---

## 8. 模拟交易（Dry-Run）

在投入真金白银之前，先用模拟资金测试策略。

### 8.1 配置模拟交易

```json
{
    "dry_run": true,
    "dry_run_wallet": 1000,
    "exchange": {
        "name": "binance",
        "key": "",
        "secret": ""
    }
}
```

### 8.2 启动模拟交易

```bash
# 基本启动
freqtrade trade --strategy MyStrategy

# 使用自定义配置
freqtrade trade --strategy MyStrategy --config user_data/config.json

# 指定日志级别
freqtrade trade --strategy MyStrategy --loglevel INFO
```

### 8.3 监控模拟交易

```bash
# 通过 Telegram 监控（需要配置 Telegram）
# 发送 /status 查看当前持仓
# 发送 /profit 查看收益

# 通过 REST API 监控
freqtrade webserver --config user_data/config.json

# 查看日志
tail -f user_data/logs/freqtrade.log
```

### 8.4 模拟交易注意事项

- 模拟交易使用实时市场数据，但不实际下单
- 手续费和滑点是模拟的，可能与实际有差异
- 建议至少运行 2-4 周再考虑实盘

---

## 9. 实盘交易（Live Trading）

### 9.1 准备工作

1. **注册交易所账户**并完成 KYC
2. **创建 API Key**，建议只开启交易权限，不开提币权限
3. **设置 IP 白名单**（如果交易所支持）
4. **充分测试模拟交易**

### 9.2 配置实盘交易

```json
{
    "dry_run": false,
    "exchange": {
        "name": "binance",
        "key": "your_api_key_here",
        "secret": "your_api_secret_here",
        "pair_whitelist": [
            "BTC/USDT",
            "ETH/USDT"
        ]
    },
    "entry_pricing": {
        "price_side": "other",
        "use_order_book": true,
        "order_book_top": 1
    },
    "exit_pricing": {
        "price_side": "other",
        "use_order_book": true,
        "order_book_top": 1
    }
}
```

### 9.3 安全建议

1. **API Key 权限最小化**：只开启交易权限
2. **设置 IP 白名单**：限制 API 访问来源
3. **从小资金开始**：先用小额资金测试
4. **设置止损**：确保策略有合理的止损
5. **定期检查**：通过 Telegram 或 WebUI 监控
6. **备份配置**：定期备份配置和策略文件

### 9.4 启动实盘交易

```bash
# 前台运行（推荐调试时使用）
freqtrade trade --strategy MyStrategy --config user_data/config.json

# 后台运行（推荐生产环境）
nohup freqtrade trade --strategy MyStrategy --config user_data/config.json &

# 使用 systemd 服务（Linux）
# 参考 docs/installation.md 中的 systemd 配置

# 使用 Docker
docker run -d \
  --name freqtrade \
  -v $(pwd)/user_data:/freqtrade/user_data \
  freqtradeorg/freqtrade:stable \
  trade --strategy MyStrategy
```

---

## 10. Telegram 远程控制

### 10.1 配置 Telegram

1. 在 Telegram 中搜索 `@BotFather`
2. 发送 `/newbot` 创建机器人
3. 获取 Bot Token
4. 获取你的 Chat ID（搜索 `@userinfobot`）

```json
{
    "telegram": {
        "enabled": true,
        "token": "your_bot_token_here",
        "chat_id": "your_chat_id_here",
        "notification_settings": {
            "status": "silent",
            "warning": "silent",
            "startup": "on",
            "entry": "on",
            "exit": "on",
            "entry_fill": "on",
            "exit_fill": "on",
            "protection_trigger": "on"
        }
    }
}
```

### 10.2 常用 Telegram 命令

| 命令 | 说明 |
|------|------|
| `/start` | 启动机器人 |
| `/stop` | 停止机器人 |
| `/status` | 查看当前持仓 |
| `/trades` | 查看交易历史 |
| `/profit` | 查看收益统计 |
| `/balance` | 查看钱包余额 |
| `/daily` | 查看每日收益 |
| `/performance` | 查看币对表现 |
| `/count` | 查看交易计数 |
| `/forcesell <id>` | 强制平仓 |
| `/forcebuy <pair>` | 强制买入 |
| `/reload_config` | 重新加载配置 |
| `/whitelist` | 查看白名单 |
| `/blacklist` | 查看黑名单 |
| `/logs` | 查看最近日志 |
| `/version` | 查看版本 |

---

## 11. FreqAI 机器学习策略

FreqAI 是 Freqtrade 内置的机器学习模块，支持训练自定义预测模型。

### 11.1 安装 FreqAI 依赖

```bash
# 基础依赖
pip install freqtrade[freqai]

# 包含 PyTorch
pip install freqtrade[freqai-rl]

# 或使用 Docker
docker build -f docker/Dockerfile.freqai -t freqtrade-freqai .
```

### 11.2 FreqAI 策略示例

```python
from freqtrade.strategy import IStrategy
from pandas import DataFrame
import talib.abstract as ta

class MyFreqAIStrategy(IStrategy):
    """
    FreqAI 策略示例
    """
    timeframe = '5m'
    can_short = True

    minimal_roi = {"0": 0.1, "240": -1}
    stoploss = -0.05
    startup_candle_count = 300

    def feature_engineering_expand_all(
        self, dataframe: DataFrame, period: int, metadata: dict, **kwargs
    ) -> DataFrame:
        """
        定义自动扩展的特征
        """
        dataframe["%-rsi-period"] = ta.RSI(dataframe, timeperiod=period)
        dataframe["%-mfi-period"] = ta.MFI(dataframe, timeperiod=period)
        dataframe["%-adx-period"] = ta.ADX(dataframe, timeperiod=period)
        dataframe["%-sma-period"] = ta.SMA(dataframe, timeperiod=period)
        dataframe["%-ema-period"] = ta.EMA(dataframe, timeperiod=period)

        return dataframe

    def feature_engineering_expand_basic(
        self, dataframe: DataFrame, metadata: dict, **kwargs
    ) -> DataFrame:
        """
        定义基本特征（不自动扩展）
        """
        dataframe["%-day_of_week"] = dataframe["date"].dt.dayofweek
        dataframe["%-hour_of_day"] = dataframe["date"].dt.hour

        return dataframe

    def feature_engineering_standard(
        self, dataframe: DataFrame, metadata: dict, **kwargs
    ) -> DataFrame:
        """
        定义标准特征
        """
        dataframe["%-raw_close"] = dataframe["close"]
        dataframe["%-raw_volume"] = dataframe["volume"]

        return dataframe

    def set_freqai_targets(
        self, dataframe: DataFrame, metadata: dict, **kwargs
    ) -> DataFrame:
        """
        定义预测目标
        """
        # 预测未来 10 根 K 线的价格变化
        dataframe["&s-close"] = (
            dataframe["close"]
            .shift(-10) / dataframe["close"] - 1
        )

        return dataframe

    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        # FreqAI 预测结果在 dataframe["&s-close"] 列
        dataframe.loc[
            (
                (dataframe["&s-close"] > 0.02) &  # 预测上涨 2%
                (dataframe["do_predict"] == 1)      # 模型可用
            ),
            "enter_long"] = 1

        dataframe.loc[
            (
                (dataframe["&s-close"] < -0.02) &  # 预测下跌 2%
                (dataframe["do_predict"] == 1)
            ),
            "enter_short"] = 1

        return dataframe

    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        dataframe.loc[
            (
                (dataframe["&s-close"] < 0) |     # 预测下跌
                (dataframe["do_predict"] == 0)      # 模型不可用
            ),
            "exit_long"] = 1

        dataframe.loc[
            (
                (dataframe["&s-close"] > 0) |
                (dataframe["do_predict"] == 0)
            ),
            "exit_short"] = 1

        return dataframe
```

### 11.3 FreqAI 配置

```json
{
    "freqai": {
        "enabled": true,
        "purge_old_models": 2,
        "train_period_days": 30,
        "identifier": "my-freqai-model",
        "feature_parameters": {
            "include_timeframes": ["5m", "15m", "1h"],
            "include_corr_pairlist": ["BTC/USDT", "ETH/USDT"],
            "indicator_periods_candles": [10, 20, 50],
            "include_shifted_candles": 2,
            "DI_threshold": 0.9
        },
        "data_split_parameters": {
            "test_size": 0.25,
            "shuffle": false
        },
        "model_training_parameters": {
            "n_estimators": 1000,
            "learning_rate": 0.01
        }
    }
}
```

### 11.4 可用的 FreqAI 模型

| 模型 | 类型 | 说明 |
|------|------|------|
| `LightGBMRegressor` | 回归 | LightGBM 梯度提升（推荐） |
| `LightGBMClassifier` | 分类 | LightGBM 分类 |
| `XGBoostRegressor` | 回归 | XGBoost 梯度提升 |
| `XGBoostClassifier` | 分类 | XGBoost 分类 |
| `XGBoostRFRegressor` | 回归 | XGBoost 随机森林 |
| `SKLearnRandomForestClassifier` | 分类 | Scikit-learn 随机森林 |
| `PyTorchMLPRegressor` | 回归 | PyTorch MLP |
| `PyTorchMLPClassifier` | 分类 | PyTorch MLP |
| `PyTorchTransformerRegressor` | 回归 | PyTorch Transformer |
| `ReinforcementLearner` | RL | 强化学习 |

```bash
# 列出所有可用模型
freqtrade list-freqaimodels
```

---

## 12. 常见问题排查

### 12.1 安装问题

**问题：TA-Lib 安装失败**

```bash
# Windows：下载预编译包
pip install --find-links https://github.com/cgohlke/talib-build/releases ta-lib

# Linux
sudo apt-get install -y build-essential
wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
tar -xzf ta-lib-0.4.0-src.tar.gz
cd ta-lib
./configure --prefix=/usr
make
sudo make install
pip install ta-lib

# macOS
brew install ta-lib
pip install ta-lib
```

**问题：numpy 版本冲突**

```bash
pip install numpy --upgrade
pip install freqtrade --force-reinstall
```

### 12.2 数据问题

**问题：回测没有交易**

- 检查数据是否存在：`freqtrade list-data`
- 检查时间范围是否正确
- 检查策略信号是否触发
- 检查 `startup_candle_count` 是否设置

**问题：数据缺失**

```bash
# 重新下载数据
freqtrade download-data --exchange binance --timerange 20230101-20240101 --erase
```

### 12.3 策略问题

**问题：策略不产生信号**

- 检查 `populate_indicators()` 是否正确计算指标
- 检查 `populate_entry_trend()` 的条件是否太严格
- 检查 `volume > 0` 条件是否满足
- 使用 `freqtrade plot-dataframe` 可视化

**问题：回测结果与实盘差异大**

- 检查滑点设置
- 检查订单类型（limit vs market）
- 检查 `startup_candle_count` 是否足够
- 确保没有使用未来数据

### 12.4 交易所问题

**问题：API 连接失败**

- 检查 API Key 和 Secret 是否正确
- 检查 API Key 权限
- 检查 IP 白名单设置
- 检查网络连接

**问题：订单失败**

- 检查余额是否足够
- 检查最小交易量
- 检查价格精度
- 检查交易所维护时间

### 12.5 性能问题

**问题：回测很慢**

- 使用更快的数据格式（Feather 或 Parquet）
- 减少币对数量
- 使用 `--cache` 缓存策略结果
- 使用更快的 CPU

**问题：内存不足**

- 减少同时处理的币对数量
- 减少 `include_corr_pairlist` 中的币对
- 使用更小的数据集

---

## 附录：常用命令速查表

### 数据相关

```bash
freqtrade download-data                          # 下载数据
freqtrade list-data                              # 列出数据
freqtrade convert-data                           # 转换数据格式
freqtrade trades-to-ohlcv                        # 交易数据转 OHLCV
freqtrade list-trades-data                       # 列出交易数据
```

### 回测相关

```bash
freqtrade backtesting                            # 运行回测
freqtrade backtesting-show                       # 显示回测结果
freqtrade backtesting-analysis                   # 分析回测结果
freqtrade plot-dataframe                         # 绘制 K 线图
freqtrade plot-profit                            # 绘制收益图
```

### 超参优化相关

```bash
freqtrade hyperopt                               # 运行超参优化
freqtrade hyperopt-list                          # 列出优化结果
freqtrade hyperopt-show                          # 显示优化详情
freqtrade list-hyperoptloss                      # 列出损失函数
```

### 交易相关

```bash
freqtrade trade                                  # 启动交易
freqtrade webserver                              # 启动 Web 服务器
freqtrade install-ui                             # 安装 FreqUI
```

### 工具相关

```bash
freqtrade create-userdir                         # 创建用户目录
freqtrade new-strategy                           # 创建新策略
freqtrade new-config                             # 创建新配置
freqtrade list-strategies                        # 列出策略
freqtrade list-exchanges                         # 列出交易所
freqtrade list-markets                           # 列出市场
freqtrade list-timeframes                        # 列出时间框架
freqtrade list-freqaimodels                      # 列出 FreqAI 模型
freqtrade test-pairlist                          # 测试币对列表
freqtrade show-config                            # 显示配置
freqtrade strategy-updater                       # 更新策略代码
```

---

## 更多资源

- [官方文档](https://www.freqtrade.io)
- [GitHub 仓库](https://github.com/freqtrade/freqtrade)
- [Discord 社区](https://discord.gg/p7nuUNVfP7)
- [策略仓库](https://github.com/freqtrade/freqtrade-strategies)
- [FreqUI 界面](https://github.com/freqtrade/freq-ui)